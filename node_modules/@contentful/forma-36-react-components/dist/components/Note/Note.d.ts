import React from 'react';
import type { CSSProperties } from 'react';
export interface NoteProps {
    noteType?: 'primary' | 'positive' | 'negative' | 'warning';
    className?: string;
    title?: string;
    style?: CSSProperties;
    testId?: string;
    children: React.ReactNode;
    hasCloseButton?: boolean;
    onClose?: Function;
}
export declare function Note({ children, className, hasCloseButton, noteType, onClose, style, testId, title, }: NoteProps): React.ReactElement;
