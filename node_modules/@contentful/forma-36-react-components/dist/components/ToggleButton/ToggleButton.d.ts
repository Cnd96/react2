import React from 'react';
import type { IconType } from '../Icon';
export interface ToggleButtonProps {
    children: React.ReactNode;
    icon?: IconType;
    isActive?: boolean;
    onToggle?: Function;
    isDisabled?: boolean;
    testId?: string;
    className?: string;
}
export declare const ToggleButton: {
    ({ children, className, icon, isActive, isDisabled, onToggle, testId, ...otherProps }: ToggleButtonProps): JSX.Element;
    Group({ className, testId, children, }: {
        className?: string;
        testId?: string;
        children: React.ReactElement<ToggleButtonProps>[];
    }): JSX.Element;
};
