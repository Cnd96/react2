import React from 'react';
import type { AssetState } from '../Card/AssetCard/AssetCard';
export declare const types: {
    archive: string;
    audio: string;
    code: string;
    image: string;
    markup: string;
    pdf: string;
    plaintext: string;
    presentation: string;
    richtext: string;
    spreadsheet: string;
    video: string;
};
export declare function isAssetType(type: string): type is AssetType;
export declare type AssetType = keyof typeof types;
export interface AssetProps {
    /**
     * Class names to be appended to the className prop of the Dropdown wrapper
     */
    className?: string;
    /**
     * A `src` attribute to use for image assets
     */
    src?: string;
    /**
     * The publish status of the asset
     */
    status?: AssetState;
    /**
     * An ID used for testing purposes applied as a data attribute (data-test-id)
     */
    testId?: string;
    /**
     * The title of the asset
     */
    title?: string;
    /**
     * The type of asset being represented
     */
    type?: AssetType;
}
export declare function Asset({ className, src, status, testId, title, type, ...otherProps }: AssetProps): React.ReactElement;
