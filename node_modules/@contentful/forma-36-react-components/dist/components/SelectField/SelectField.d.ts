import React from 'react';
import type { FocusEventHandler, ChangeEventHandler } from 'react';
import type { FormLabelProps } from '../FormLabel';
import type { SelectProps } from '../Select';
import type { TextLinkProps } from '../TextLink';
export interface SelectFieldProps {
    name: string;
    id: string;
    labelText: string;
    children: React.ReactNode;
    value?: string;
    validationMessage?: string;
    formLabelProps?: Partial<FormLabelProps>;
    textLinkProps?: Partial<TextLinkProps>;
    selectProps?: Partial<SelectProps>;
    helpText?: string;
    required?: boolean;
    onChange?: ChangeEventHandler<HTMLSelectElement>;
    onBlur?: FocusEventHandler<HTMLSelectElement>;
    testId?: string;
    className?: string;
}
export declare const SelectField: ({ children, className, formLabelProps, helpText, id, labelText, name, onBlur, onChange, required, selectProps, testId, textLinkProps, validationMessage, value, ...otherProps }: SelectFieldProps) => JSX.Element;
