import React from 'react';
export interface ModalHeaderProps {
    title: string;
    onClose?: Function;
    testId?: string;
    className?: string;
    isNotWrapped?: boolean;
    style?: React.CSSProperties;
}
export declare function ModalHeader({ onClose, title, testId, isNotWrapped, className, ...rest }: ModalHeaderProps): React.ReactElement;
