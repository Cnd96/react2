import { ChangeEventHandler, ReactNode } from 'react';
export interface ControlledInputFieldPropTypes {
    id: string;
    labelText: string;
    labelIsLight?: boolean;
    required?: boolean;
    helpText?: string;
    formLabelProps?: object;
    disabled?: boolean;
    helpTextProps?: object;
    validationMessage?: string;
    value?: string;
    name?: string;
    checked?: boolean;
    inputProps?: object;
    inputType?: 'radio' | 'checkbox';
    onChange?: ChangeEventHandler<HTMLInputElement>;
    className?: string;
    testId?: string;
    children?: ReactNode;
}
export declare const ControlledInputField: ({ id, labelIsLight, testId, required, helpText, disabled, labelText, helpTextProps, formLabelProps, className, checked, value, validationMessage, onChange, children, inputType, inputProps, name, ...otherProps }: ControlledInputFieldPropTypes) => JSX.Element;
