import React from 'react';
import type { ButtonHTMLAttributes } from 'react';
import { sortingDirections } from '../TableCell/TableCell';
export interface TableSortingLabelProps extends ButtonHTMLAttributes<HTMLButtonElement> {
    children: React.ReactNode;
    direction: keyof typeof sortingDirections;
    active: boolean;
    className?: string;
    testId?: string;
}
export declare const TableSortingLabel: ({ active, children, className, direction, testId, ...otherProps }: TableSortingLabelProps) => JSX.Element;
