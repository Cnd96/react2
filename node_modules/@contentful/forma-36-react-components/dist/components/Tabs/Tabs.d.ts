import React from 'react';
import type { CSSProperties } from 'react';
export interface TabsProps {
    dividerOrientation?: 'horizontal' | 'vertical';
    role?: 'navigation' | 'tablist';
    style?: CSSProperties;
    className?: string;
    withDivider?: boolean;
    children?: React.ReactNode;
    testId?: string;
}
export declare function Tabs({ className, children, dividerOrientation, testId, role, withDivider, style, }: TabsProps): React.ReactElement;
