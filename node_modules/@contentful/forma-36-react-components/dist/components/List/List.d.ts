import React from 'react';
declare type ListType = 'ul' | 'ol';
export interface ListProps {
    element?: ListType;
    className?: string;
    children: React.ReactNode;
    style?: React.CSSProperties;
    testId?: string;
}
export declare function List({ element: Tag, className, children, testId, ...otherProps }: ListProps): React.ReactElement;
export {};
