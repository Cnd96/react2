import React from 'react';
export interface ListItemProps {
    className?: string;
    children: React.ReactNode;
    testId?: string;
}
export declare function ListItem({ className, children, testId, ...otherProps }: ListItemProps): React.ReactElement;
