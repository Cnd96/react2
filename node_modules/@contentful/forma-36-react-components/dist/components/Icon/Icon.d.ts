import React from 'react';
import type { CSSProperties } from 'react';
import { iconName } from './constants';
export declare type IconType = keyof typeof iconName;
export interface IconProps extends React.SVGProps<SVGSVGElement> {
    size?: 'tiny' | 'small' | 'medium' | 'large';
    color?: 'primary' | 'positive' | 'negative' | 'warning' | 'secondary' | 'muted' | 'white';
    style?: CSSProperties;
    icon: IconType;
    className?: string;
    testId?: string;
}
export declare function Icon({ className, icon, size, color, testId, ...otherProps }: IconProps): React.ReactElement;
