import React from 'react';
export interface FormLabelProps {
    htmlFor: string;
    children: React.ReactNode;
    testId?: string;
    className?: string;
    requiredText?: string;
    required?: boolean;
}
export declare const FormLabel: ({ children, className, htmlFor, required, requiredText, testId, ...otherProps }: FormLabelProps) => JSX.Element;
