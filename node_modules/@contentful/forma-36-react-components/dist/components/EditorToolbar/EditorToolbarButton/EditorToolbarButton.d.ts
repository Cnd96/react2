import React from 'react';
import type { MouseEventHandler } from 'react';
import { TooltipPlace } from '../../Tooltip';
import type { IconButtonProps } from '../../IconButton';
import type { IconType } from '../../Icon';
export interface EditorToolbarButtonProps {
    label: string;
    icon: IconType;
    tooltip?: string;
    tooltipPlace?: TooltipPlace;
    iconButtonProps?: Partial<IconButtonProps>;
    isActive?: boolean;
    disabled?: boolean;
    onClick?: MouseEventHandler;
    withDropdown?: boolean;
    className?: string;
    testId?: string;
}
export declare function EditorToolbarButton({ className, label, testId, icon, tooltip, tooltipPlace, iconButtonProps, isActive, disabled, onClick, withDropdown, ...otherProps }: EditorToolbarButtonProps): React.ReactElement;
