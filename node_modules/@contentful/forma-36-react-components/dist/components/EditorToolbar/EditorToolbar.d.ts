import React from 'react';
export interface EditorToolbarProps {
    className?: string;
    children: React.ReactNode;
    testId?: string;
    style?: React.CSSProperties;
}
export declare function EditorToolbar({ className, children, testId, ...otherProps }: EditorToolbarProps): React.ReactElement;
