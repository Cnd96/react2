import React from 'react';
declare type stringOrNumber = string | number;
export interface SkeletonTextProps {
    numberOfLines?: number;
    offsetTop?: stringOrNumber;
    offsetLeft?: stringOrNumber;
    lineHeight?: stringOrNumber;
    marginBottom?: stringOrNumber;
    width?: stringOrNumber;
}
export declare function SkeletonText({ numberOfLines, offsetLeft, offsetTop, lineHeight, marginBottom, width, }: SkeletonTextProps): React.ReactElement;
export {};
