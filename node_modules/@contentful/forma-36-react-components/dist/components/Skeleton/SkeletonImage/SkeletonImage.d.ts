import React from 'react';
declare type stringOrNumber = string | number;
export interface SkeletonImageProps {
    width?: stringOrNumber;
    height?: stringOrNumber;
    radiusX?: stringOrNumber;
    radiusY?: stringOrNumber;
    offsetLeft?: stringOrNumber;
    offsetTop?: stringOrNumber;
    testId?: string;
}
export declare function SkeletonImage({ testId, offsetLeft, offsetTop, width, height, radiusX, radiusY, ...otherProps }: SkeletonImageProps): React.ReactElement;
export {};
