import React from 'react';
export interface HelpTextProps {
    className?: string;
    testId?: string;
    style?: React.CSSProperties;
    children: React.ReactNode;
}
export declare function HelpText({ className, children, testId, ...otherProps }: HelpTextProps): React.ReactElement;
