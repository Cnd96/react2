import { CSSProperties, FormEventHandler, ReactChild, ReactNodeArray } from 'react';
export interface FormProps {
    onSubmit?: FormEventHandler;
    spacing?: 'condensed' | 'default';
    testId?: string;
    style?: CSSProperties;
    className?: string;
    children: ReactChild | ReactNodeArray;
}
export declare const Form: ({ children, className, onSubmit, spacing, testId, ...otherProps }: FormProps) => JSX.Element;
